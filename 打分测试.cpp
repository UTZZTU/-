#include <stdio.h>
int main ()
{
    int c[100];
    int n;
	int sum=0,w=0,tm,s=100; 
    scanf("%d",&n);
    int i;
    for (i=0;i<n;i++)
		{
			
			int c[i];
			scanf("%d",&c[i]);
			for(i=0;i<n;i++)
		{
		    
			if(c[a]>w)
			w=c[a];
			
        }
        for(int b=0;b<n;b++)
        {
        	
        	if(c[b])<s)
        	s=c[b];
		}
			sum+=c[i];
		}
		
		
        tm=sum-w;
        printf("%d",tm);
			return 0; 
}
