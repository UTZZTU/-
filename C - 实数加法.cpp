#include <stdio.h>
#include <string.h>
int max(int a,int b)
{
	if(a>=b)
	return a;
	else
	return b;
}
int main ()
{
	char s1[110],s2[110];
	int q1[105],q2[105],h1[105],h2[105],q[105],h[105],i,j,a1,a2,b1,b2,jw,zd;
     gets(s1);
     gets(s2);
     memset(q1,0,sizeof(q1));
     memset(q2,0,sizeof(q2));
     memset(h1,0,sizeof(h1));
     memset(h2,0,sizeof(h2));
     memset(q,0,sizeof(q));
     memset(h,0,sizeof(h));
     j=0;
     for(i=0;s1[i]!='\0';i++)
     {
     	if(s1[i]=='.')
     	break;
     	else
     	q1[i]=s1[i]-'0';
	 }
	 a1=i;
	 i++;
	 for(i;s1[i]!='\0';i++)
	 {
	 	h1[j]=s1[i]-'0';
	 	j++;
	 }
	 b1=j;
	 j=0;
     for(i=0;s2[i]!='\0';i++)
     {
     	if(s2[i]=='.')
     	break;
     	else
     	q2[i]=s2[i]-'0';
	 }
	 a2=i;
	 i++;
	 for(i;s2[i]!='\0';i++)
	 {
	 	h2[j]=s2[i]-'0';
	 	j++;
	 }
	 b2=j;
	 jw=0;
	 if(b1>=b2)
	 {
	 	for(i=b1-1;i>=0;i--)
	 	{
	 		h[i]=h1[i]+h2[i]+jw;
	 		jw=h[i]/10;
	 		h[i]%=10;
		 }
		 if(jw==1)
		 {
		 	zd=max(a1,a2);
		 	q[zd]+=1;
		 }
	 }
	 else
	 {
	 	for(j=b2-1;j>=0;j--)
	 	{
	 		h[j]=h1[j]+h2[j]+jw;
	 		jw=h[j]/10;
	 		h[j]%=10;
		 }
		 if(jw==1)
		 {
		 	zd=max(a1,a2);
		 	q[zd]+=1;
		 }
	 }
	 jw=0;
	 if(a1>=a2)
	 {
	 	for(i=a1,j=a2;i>=1&&j>=1;i--,j--)
	 	{
	 		q[i]+=q1[i-1]+q2[j-1]+jw;
	 		jw=q[i]/10;
	 		q[i]%=10;
		 }
		 for(i;i>=1;i--)
		 {
		 	q[i]+=q1[i-1]+jw;
		 	jw=q[i]/10;
		 	q[i]%=10;
		 }
		 q[i]+=jw;
	 }
	 else
	 {
	 	for(i=a1,j=a2;i>=1&&j>=1;i--,j--)
	 	{
	 		q[j]+=q1[i-1]+q2[j-1]+jw;
	 		jw=q[j]/10;
	 		q[j]%=10;
		 }
		 for(j;j>=1;j--)
		 {
		 	q[j]+=q2[j-1]+jw;
		 	jw=q[j]/10;
		 	q[j]%=10;
		 }
		 q[j]+=jw;
	 }
	 for(i=0;i<max(a1,a2)&&q[i]==0;i++)
	 ;
	 for(i;i<=max(a1,a2);i++)
	 printf("%d",q[i]);
	 for(j=max(b1,b2)-1;j>=0&&h[j]==0;j--)
	 ;
	 if(j!=-1)
	 {
	 	printf(".");
	 for(i=0;i<=j;i++)
	 {
	 	printf("%d",h[i]);
	 }
	 }
	 return 0;
}
